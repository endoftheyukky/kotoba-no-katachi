# Cloudflare Pages: the published site (static, direct upload from dist/).
# The project name is only the address (kotoba-no-katachi.pages.dev); the page shows no name.
name = "kotoba-no-katachi"
pages_build_output_dir = "./dist"
compatibility_date = "2026-09-22"
